SELECT
    s.Title as songTitle,
    m.Title as movieTitle,
    count (a.full_name) as artistNameCount
FROM
    song s
    left join movie m on s.movie_id = m.id
    left join song_has_artist sha on s.id = sha.song_id
    left join artist a on a.id = sha.artist_id
GROUP BY
    S.TITLE



